# The agent-workday patrol runs every standing agent's workday in sequence, each
# a model run of several minutes; the template's 20 minute start timeout killed it
# after two agents. Each agent is bounded by its own workday.max_minutes.
[Service]
TimeoutStartSec=6h
